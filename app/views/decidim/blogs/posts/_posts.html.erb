<div class="row small-up-1 medium-up-3 card-grid">
  <% posts.each do |post| %>
    <div class="column">
      <article class="card card--blog">
        <div class="card__content">
          <%= link_to post, class: "card__link" do %>
            <h5 class="card__title"><%= translated_attribute post.title %></h5>
          <% end %>
          <%= render partial: "datetime", locals: { post: post } %>
          <%= sanitize post_description(post) %>
        </div>
      </article>
    </div>
  <% end %>
</div>
<%= decidim_paginate posts %>
